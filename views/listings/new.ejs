<% layout("/layouts/boilerplate") -%>
<body>
  <br />
  <div class="row">
    <div class="col-8 offset-2">
      <h2>Add New Product</h2>
      <br />
      <form
        action="/listings"
        method="POST"
        class="needs-validation"
        enctype="multipart/form-data"
        novalidate
      >
        <div class="mb-3">
          <label class="form-label" for="title">Name of Product </label
          ><span> <b style="color: red"> *</b> : </span>
          <input
            type="text"
            class="form-control"
            width="50%"
            id="title"
            placeholder="Enter Name Of Product"
            name="listing[title]"
            pattern="[a-zA-Z0-9\-\.\()]+"
            required
          />
          <div class="invalid-feedback">Please Enter a valid Name</div>
        </div>
        <div class="mb-3">
          <label class="form-label" for="desc">Description </label
          ><span> <b style="color: red"> *</b> : </span>
          <textarea
            name="listing[description]"
            class="form-control"
            id="desc"
            rows="5"
            cols="30"
            placeholder="Enter the description of the Product"
            required
          ></textarea>
          <div class="invalid-feedback">
            Please provide the valid Description
          </div>
        </div>
        <div class="mb-3">
          <label class="form-label" for="img">Upload Image of Product</label
          ><span> <b style="color: red"> *</b> : </span>
          <input
            type="file"
            class="form-control"
            width="50%"
            accept="image/jpeg, image/png, image/jpg"
            placeholder="Browse Image to Upload"
            width="500px"
            id="img"
            name="listing[image]"
            required
          />
          <div class="invalid-feedback">
            Please Select image of type: jpeg, png only
          </div>
        </div>

        <div class="row">
          <div class="mb-3 col-md-4">
            <label class="form-label" for="price">Select Category </label
            ><span> <b style="color: red"> *</b> : </span>
            <select
              class="form-select"
              name="listing[category]"
              id="category"
              onchange="setTax(this.value)"
            >
              <option value="" selected disabled>Select Category</option>
              <option value="Beverages">Beverages (12% Tax)</option>
              <option value="Snacks">Snacks and Sweets (18% Tax)</option>
              <option value="Dairy">Dairy & Eggs (2% Tax)</option>
              <option value="Grocery">Grocery (5% Tax)</option>
            </select>
            <div class="invalid-feedback">Please Select the valid Category</div>
          </div>
          <div class="col-md-4 mb-3">
            <label class="form-label" for="quant">Select Quantity Type</label
            ><span><b style="color: red"> *</b> : </span>
            <select
              name="listing[quantityType]"
              class="form-select"
              id="quant"
              required
            >
              <option value="" selected disabled>Select Quantities</option>
              <!-- <option value="loose_weight">Loose Weight</option>
                <option value="single_variant">Single Variant</option> -->
              <option value="fixed_pack">Fixed Pack</option>
              <option value="count">Count</option>
            </select>
            <div class="invalid-feedback">Please Select the valid Quantity</div>
          </div>
          <div class="col-md-4 mb-3" id="stockValCount">
            <label for="count" class="form-label"
              >Number Of Stock Available </label
            ><span> <b style="color: red"> *</b> : </span>
            <input
              type="Number"
              class="form-control"
              placeholder="Enter the Quantity of Stock"
              id="count"
              oninput="if (this.value < 0) this.value = 0;"
              name="listing[stockCount]"
              required
            />
            <div class="invalid-feedback">
              Please Provide the Quntatiy of Stock
            </div>
          </div>
          <div class="mb-3 col-md-4 d-none" id="innerTax">
            <label class="form-label" for="taxField">Tax on Product %: </label
            ><span> <b style="color: red"> *</b> : </span>
            <input
              type="number"
              min="0"
              class="form-control"
              width="50%"
              placeholder="Select Category for Tax"
              id="taxFieldInner"
              oninput="if (this.value < 0) this.value = 0;"
              required
              readonly
            />
            <div class="invalid-feedback">
              Please Select valid Category for Tax Selection
            </div>
          </div>
        </div>

        <div id="count_fields" class="quant-fields d-none mb-3">
          <label class="form-label">Count Variants (e.g. 6 pcs, 1 Dozen)</label>
          <div id="count_variant_rows"></div>
          <button
            type="button"
            class="btn btn-sm btn-outline-dark mt-2"
            onclick="addVariantRow('count')"
          >
            + Add Variant
          </button>
        </div>

        <div class="row baseCostSection">
          <div class="divider costSection mt-4 mb-4">
            <span
              style="
                margin-left: 330px;
                color: rgb(137, 132, 132);
                font-weight: 700;
                font-size: 20px;
              "
              ><u>Cost Section</u></span
            >
            <!-- <hr> -->
          </div>
          <div class="mb-3 col-md-4" id="costPrice">
            <label class="form-label" for="price">Cost Price </label
            ><span> <b style="color: red"> *</b> : </span>
            <input
              type="number"
              min="0"
              class="form-control"
              width="50%"
              placeholder="Enter Cost price of Product"
              id="price"
              name="listing[costPrice]"
              oninput="if (this.value < 0) this.value = 0;"
              required
            />
            <div class="invalid-feedback">
              Please Provide the valid Cost Price
            </div>
          </div>
          <div class="mb-3 col-md-4" id="tax">
            <label class="form-label" for="taxField">Tax on Product %: </label
            ><span> <b style="color: red"> *</b> : </span>
            <input
              type="number"
              min="0"
              class="form-control"
              width="50%"
              placeholder="Select Category for Tax"
              id="taxField"
              name="listing[Tax]"
              oninput="if (this.value < 0) this.value = 0;"
              required
              readonly
            />
            <div class="invalid-feedback">
              Please Select valid Category for Tax Selection
            </div>
          </div>
          <div class="mb-3 col-md-4" id="profitMargin">
            <label class="form-label" for="price">Profit Margin %: </label
            ><span> <b style="color: red"> *</b> : </span>
            <input
              type="number"
              min="0"
              class="form-control"
              width="50%"
              placeholder="Enter Margin Percentage"
              id="margin"
              name="listing[margin]"
              oninput="if (this.value < 0) this.value = 0;"
              required
            />
            <div class="mt-2" style="font-size: 13px">
              Profit Margin is Considered in <b>Percentage</b
              ><span style="color: red">*</span>
            </div>
            <div class="invalid-feedback">
              Please Provide the valid Profit Margin
            </div>
          </div>
        </div>
        <div class="mb-3">
          <label class="form-label" for="loc"
            >Special Feature [USP] :
            <i class="fa-solid fa-chess-king" style="color: gold"></i
          ></label>
          <!-- <span> <b style="color: red"> *</b> : </span> -->
          <input
            type="text"
            class="form-control"
            width="50%"
            placeholder="Enter the USP of Product (Optional)"
            id="loc"
            name="listing[location]"
            value=""
          />
          <div class="invalid-feedback">
            Please provide the Valid Imported Location
          </div>
        </div>
        <div class="new-btn">
          <button class="btn btn-outline-dark mb-3 mt-3 add">
            Add Product
          </button>
        </div>
        <br /><br />
      </form>
    </div>
  </div>
  <div class="loader-overlay" id="addProductLoaderOverlay">
    <div class="loader-container">
      <div class="loader"></div>
      <p class="loader-text">Adding New Product ...</p>
    </div>
  </div>
</body>
<script>
  document.getElementById("title").addEventListener("input", (e) => {
    e.target.value = e.target.value.replace(/[^a-zA-Z0-9\-\.( )]/g, "");
  });
  document.getElementById("desc").addEventListener("input", (e) => {
    e.target.value = e.target.value.replace(/[^a-zA-Z0-9 \-\.()]/g, "");
  });

  const quantSelect = document.getElementById("quant");
  const allQuantFieldGroups = document.querySelectorAll(".quant-fields");
  const baseCostSection = document.querySelector(".baseCostSection");
  const costPrice = document.querySelector("#costPrice"); //
  const profitMargin = document.querySelector("#profitMargin"); //
  const stockInput = document.getElementById("count");
  const taxDiv = document.getElementById("innerTax");
  const stockInputDiv = document.getElementById("stockValCount"); //
  const costPriceInput = document.getElementById("price"); //
  const marginInput = document.getElementById("margin");
  const mainTaxInput = document.getElementById("taxField");
  const innerTaxInput = document.getElementById("taxFieldInner");

  quantSelect.addEventListener("change", function () {
    allQuantFieldGroups.forEach((el) => el.classList.add("d-none"));
    const selected = this.value;
    let target = document.getElementById(selected + "_fields");

    if (target) target.classList.remove("d-none");
    // Ensure only the visible tax input carries the form name so server receives a single value
    if (mainTaxInput && innerTaxInput) {
      if (selected === "count") {
        // use inner tax input for count mode
        innerTaxInput.name = "listing[Tax]";
        // remove name from main input to avoid duplicate values
        mainTaxInput.removeAttribute("name");
        // copy value so visible input shows current tax
        innerTaxInput.value = mainTaxInput.value || innerTaxInput.value || "";
      } else {
        // use main tax input for fixed pack mode
        mainTaxInput.name = "listing[Tax]";
        innerTaxInput.removeAttribute("name");
        mainTaxInput.value = innerTaxInput.value || mainTaxInput.value || "";
      }
    }
    if (selected === "count") {
      baseCostSection.classList.add("d-none");
      stockInputDiv.classList.add("d-none");
      taxDiv.classList.remove("d-none");
      costPrice.classList.add("d-none");
      profitMargin.classList.add("d-none");
      // taxDiv.classList.remove("d-none"); // show tax option for all product
      stockInputDiv.classList.add("d-none");
      stockInput.required = false;
      costPriceInput.required = false;
      marginInput.required = false;
    } else {
      baseCostSection.classList.remove("d-none");
      stockInputDiv.classList.remove("d-none");
      taxDiv.classList.add("d-none");
      costPrice.classList.remove("d-none");
      profitMargin.classList.remove("d-none");
      // taxDiv.classList.remove("d-none"); // show tax option for all product
      stockInputDiv.classList.remove("d-none");
      stockInput.required = true;
      costPriceInput.required = true;
      marginInput.required = true;
    }
  });

  let variantIndex = { count: 0, fixed_pack: 0 };

  function addVariantRow(type) {
    const container = document.getElementById(type + "_variant_rows");
    const idx = variantIndex[type]++;
    const row = document.createElement("div");
    row.className = "row g-2 mb-2 align-items-center";
    row.innerHTML = `
      <div class="row mt-3">
        <hr>
        <div class="col-md-4 mb-3">
          <input type="text" name="listing[variants][${idx}][label]" class="form-control" placeholder="Label e.g. 500g / 1 Dozen" required>
        </div>
        <div class="col-md-4" mb-3>
          <select name="listing[variants][${idx}][unit]" class="form-select">
            <option value="kg">Kilogram</option>
            <option value="g">gram</option>
            <option value="l">Litre</option>
            <option value="ml">Mili-litre</option>
            <option value="dozen">Dozen</option>
            <option value="sack">Sack</option>
          </select>
        </div>
        <div class="col-md-2 mb-3">
          <button type="button" class="btn btn-sm btn-danger" onclick="this.closest('.row').remove()">Remove ✕</button>
        </div>
        <div class="mb-3 col-md-4" id="costPrice">
          <label class="form-label" for="price">Cost Price </label
          ><span> <b style="color: red"> *</b> : </span>
          <input
            type="number"
            min="0"
            class="form-control"
            width="50%"
            placeholder="Enter Cost price of Product"
            id="price"
            name="listing[variants][${idx}][costPrice]"
            oninput="if (this.value < 0) this.value = 0;"
            required
          />
          <div class="invalid-feedback">
            Please Provide the valid Cost Price
          </div>
        </div>
        <div class="mb-3 col-md-4" id="profitMargin">
          <label class="form-label" for="price">Profit Margin %: </label
          ><span> <b style="color: red"> *</b> : </span>
          <input
            type="number"
            min="0"
            class="form-control"
            width="50%"
            placeholder="Enter Margin Percentage"
            id="margin"
            name="listing[variants][${idx}][margin]"
            oninput="if (this.value < 0) this.value = 0;"
            required
          />
          <div class="mt-2" style="font-size: 13px">
            Profit Margin is Considered in <b>Percentage</b
            ><span style="color: red">*</span>
          </div>
          <div class="invalid-feedback">
            Please Provide the valid Profit Margin
          </div>
        </div>
      
        <div class="col-md-4 mb-3" id="stockValCount">
          <label for="count" class="form-label"
            >Stock Count</label
          ><span> <b style="color: red"> *</b> : </span>
          <input
            type="Number"
            class="form-control"
            placeholder="Enter the Quantity of Stock"
            id="count"
            oninput="if (this.value < 0) this.value = 0;"
            name="listing[variants][${idx}][stockCount]"
            required
          />
          <div class="invalid-feedback">
            Please Provide the Quntatiy of Stock
          </div>
        </div>
        <hr>
      </div>
      
    `;
    container.appendChild(row);
  }
</script>
